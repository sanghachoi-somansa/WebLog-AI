# ---------------------------------------------------------------------------
# SSH: 선택 사항(개발 편의). 실제 접속 정보는 앱 사이드바에서 입력·변경합니다.
# ---------------------------------------------------------------------------
SSH_HOST=
SSH_USER=
SSH_PASSWORD=
LOG_PATH=/var/log/auth.log

# ---------------------------------------------------------------------------
# LLM: Base URL은 core/config.py 고정. 모델 id는 /v1/models 와 일치해야 함.
# 기본값 gemma4:26b — 다른 id 쓰려면 예: VLLM_MODEL=gemma4
# ---------------------------------------------------------------------------
VLLM_API_KEY=dummy
VLLM_MODEL=gemma4:26b

# LLM 호출 끄기(SSH 로그만): 1
DISABLE_LLM=0

# LLM 500 등 발생 후 같은 호출 반복 방지(초)
LLM_COOLDOWN_AFTER_ERROR=120

# ---------------------------------------------------------------------------
# Docker 배포: 서버에서 `cp .env.example .env` 후 필요한 값만 수정.
# ---------------------------------------------------------------------------
